import boto3
import csv
import os
import json
from datetime import datetime, timezone, timedelta

def main(event, context):
    # Initialize clients
    config_client = boto3.client('config')
    s3_client = boto3.client('s3')
    
    # Configuration - update these values as needed
    aggregator_name = 'YourConfigAggregatorName'  # Replace with your aggregator name
    s3_bucket = 'your-log-bucket-name'           # Replace with your S3 bucket
    s3_key_prefix = 'iam-policy-changes/'        # Replace with desired prefix
    
    # Calculate date range (last 7 days)
    end_time = datetime.now(timezone.utc)
    start_time = end_time - timedelta(days=7)
    
    # Format dates for query
    start_time_str = start_time.strftime('%Y-%m-%dT%H:%M:%S.%fZ')
    end_time_str = end_time.strftime('%Y-%m-%dT%H:%M:%S.%fZ')
    
    # Generate timestamp for the filename
    timestamp = end_time.strftime('%Y-%m-%d_%H-%M-%S')
    filename = f'iam_policy_changes_last7days_{timestamp}.csv'
    s3_key = f'{s3_key_prefix}{filename}'
    
    try:
        # Query AWS Config for IAM policy changes in last 7 days
        response = config_client.select_aggregate_resource_config(
            Expression=f"""
                SELECT
                    resourceId,
                    resourceType,
                    configurationItemCaptureTime,
                    configurationItemStatus,
                    configurationStateId,
                    awsAccountId,
                    awsRegion,
                    configurationItemDiff
                WHERE
                    resourceType = 'AWS::IAM::Policy' AND
                    configurationItemCaptureTime >= '{start_time_str}' AND
                    configurationItemCaptureTime <= '{end_time_str}'
                ORDER BY
                    configurationItemCaptureTime DESC
            """,
            ConfigurationAggregatorName=aggregator_name,
            Limit=1000  # Adjust based on expected volume
        )
        
        # Process the results
        results = response.get('Results', [])
        
        if not results:
            print(f"No IAM policy changes found between {start_time_str} and {end_time_str}.")
            return {
                'statusCode': 200,
                'body': f'No IAM policy changes found between {start_time_str} and {end_time_str}.'
            }
        
        # Prepare CSV data
        csv_rows = []
        headers = [
            'AccountID', 'Region', 'ResourceID', 'ResourceType', 
            'CaptureTime', 'Status', 'StateID', 'ChangeType', 'ChangedProperties'
        ]
        
        for result in results:
            item = json.loads(result)
            diff = item.get('configurationItemDiff', {})
            change_type = diff.get('changeType', 'N/A')
            changed_properties = ', '.join(diff.get('changedProperties', []))
            
            csv_rows.append([
                item.get('awsAccountId', 'N/A'),
                item.get('awsRegion', 'N/A'),
                item.get('resourceId', 'N/A'),
                item.get('resourceType', 'N/A'),
                item.get('configurationItemCaptureTime', 'N/A'),
                item.get('configurationItemStatus', 'N/A'),
                item.get('configurationStateId', 'N/A'),
                change_type,
                changed_properties
            ])
        
        # Write to CSV in memory
        csv_buffer = []
        writer = csv.writer(csv_buffer)
        writer.writerow(headers)
        writer.writerows(csv_rows)
        csv_content = '\n'.join(csv_buffer)
        
        # Upload to S3
        s3_client.put_object(
            Bucket=s3_bucket,
            Key=s3_key,
            Body=csv_content
        )
        
        print(f"Successfully saved {len(csv_rows)} IAM policy changes to s3://{s3_bucket}/{s3_key}")
        print(f"Time range: {start_time_str} to {end_time_str}")
        
        return {
            'statusCode': 200,
            'body': f"Successfully processed {len(csv_rows)} IAM policy changes from last 7 days.",
            's3_location': f"s3://{s3_bucket}/{s3_key}",
            'time_range': {
                'start': start_time_str,
                'end': end_time_str
            }
        }
        
    except Exception as e:
        print(f"Error processing IAM policy changes: {str(e)}")
        raise e
